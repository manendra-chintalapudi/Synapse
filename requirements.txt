# Synapse backend — pinned runtime dependencies (Python 3.11+).
# Trino is intentionally REMOVED: structured/federated queries now run directly via duckdb.
# torch is CPU-only for inference; on Railway/Nixpacks the Linux CPU wheel is installed.

# --- web server ---
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.13.4

# --- knowledge graph ---
neo4j==6.2.0

# --- structured federation (direct, no Trino) ---
duckdb==1.5.4

# --- document retrieval (vector store + embeddings) ---
chromadb==1.5.9
langchain==1.3.11
langchain-core==1.4.8
langchain-chroma==1.1.0
langchain-huggingface==1.2.2
sentence-transformers==5.6.0
transformers==5.13.0
torch==2.12.1
numpy==2.5.1

# --- OpenRouter HTTP (router tier-3 + synthesizer) ---
requests==2.34.2
